import styled from "styled-components";

export const BoosterContainer = styled.a`
  max-width: 200px;

  img {
    width: 100%;
  }
  &:hover {
    cursor: pointer;
    transition: 0.2s ease-in-out;
  }
`;
