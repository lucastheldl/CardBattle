import styled from "styled-components";

export const CardContainer = styled.div`
  display: flex;

  flex-direction: column;

  border-radius: 6px;
  padding: 0.5rem;

  img {
    width: 150px;
    object-fit: cover;
  }
`;
